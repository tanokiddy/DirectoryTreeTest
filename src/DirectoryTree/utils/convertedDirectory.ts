export const abc = {}  
  // OLD LOOP
  
  // for (const item of input) {
  //   if (item.parentDirectoryId === parentId) {
  //     const newItem = { ...item };
  //     newItem.children = convertArrayToHierachyData(
  //       input,
  //       newItem.directoryId
  //     );
  //     result.push(newItem);
  //   }
  // }